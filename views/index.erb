<html>
  <head>
    <style>
      body {
        <% if guess > number %>
          background: #f55;
        <% elsif guess < number %>
          background: #f99;
        <% else %>
          background: #9f9;
        <% end %>
      }
    </style>
  </head>
  <body>
    <p>
      <% if guess == number || cheat == 'true' %>
        THE SECRET NUMBER IS  <%= number %>
      <% end %>
    </p>  
    <p>
      <%= message %>
    </p>

    <p>
      You have <%= no_guesses %> guesses left!
    </p>
    <form>
      <input type='text' name='guess' />
      <input type='submit' value='Guess!' />
    </form>
  </body>
</html>